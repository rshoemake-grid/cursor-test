apiVersion: v1
kind: Service
metadata:
  name: workflow-builder-frontend
  namespace: workflow-builder
  labels:
    app: workflow-builder-frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: workflow-builder-frontend
