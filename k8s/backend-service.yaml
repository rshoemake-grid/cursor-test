apiVersion: v1
kind: Service
metadata:
  name: workflow-builder-backend
  namespace: workflow-builder
  labels:
    app: workflow-builder-backend
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: workflow-builder-backend
