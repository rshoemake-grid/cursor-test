# CRA dev server proxies /api to PROXY_TARGET (see src/setupProxy.js). Python and Java both use port 8000;
# run only one backend at a time. Java: cd backend-java && ./gradlew bootRun. Python: uvicorn on 127.0.0.1:8000.
# For a direct API URL instead, set e.g. REACT_APP_API_BASE_URL=http://127.0.0.1:8000 in .env.development.local
REACT_APP_API_BASE_URL=
PROXY_TARGET=http://127.0.0.1:8000

# Match `npm run lint` when running the dev server (same as .env.production for builds).
EXTEND_ESLINT=true
