# Default: same-origin /api (CRA dev server proxies to FastAPI). Avoids CORS and localhost vs 127.0.0.1 mismatches.
# For a direct API URL instead, set e.g. REACT_APP_API_BASE_URL=http://127.0.0.1:8000 in .env.development.local
REACT_APP_API_BASE_URL=
