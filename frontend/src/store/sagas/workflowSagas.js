/**
 * Workflow domain sagas (reserved for async workflow operations).
 * State updates are synchronous via workflowReducer today.
 */
export function* workflowSagas() {
  // no-op root for future async workflow I/O
}
